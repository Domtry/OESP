services:
  
  # Web App (React + Vite)
  web-app:
    build:
      context: .
      dockerfile: .Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - sync-server
    restart: unless-stopped
