version: '3.8'

services:
  
  # Web App (React + Vite)
  web-app:
    build:
      context: .
      dockerfile: oesp-ts-test-app/Dockerfile
    ports:
      - "3000:80"
    depends_on:
      - sync-server
    restart: unless-stopped
